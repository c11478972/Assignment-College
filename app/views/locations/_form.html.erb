<%= form_for(@location) do |f| %>
  <% if @location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
      <% @location.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :postal_code %><br />
    <%= f.select :postal_code, ["Dublin","Cork","Wexford","Wicklow","Kerry","London Derry","Belfast","Longford","Meath","Roscommon","Galway","Limerick","Scotland","England","Wales","Donegal","Queen's County","Kilkenny"] %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
