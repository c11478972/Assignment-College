<h1>Listing repairs</h1>

<table>
  <tr>
    <th>Vehicle</th>
    <th>Service station</th>
    <th>End date</th>
    <th>Cost €</th>
	<th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @repairs.each do |repair| %>
  <tr>
    <td><%= repair.vehicle.registration_num %></td>
    <td><%= repair.service_station.name %></td>
    <td><%= repair.end_date %></td>
	<td><%= repair.cost %></td>
    <td><%= repair.description %></td>
    <td><%= link_to 'Show', repair %></td>
    <td><%= link_to 'Edit', edit_repair_path(repair) %></td>
    <td><%= link_to 'Destroy', repair, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Repair', new_repair_path %>
